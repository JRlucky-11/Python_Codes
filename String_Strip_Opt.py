a = "*Python*"
print(a.strip('*'))  # This will remove the leading and trailing whitespace from the string
print(a.lstrip('*'))  # This will remove the leading whitespace from the string
print(a.rstrip('*'))  # This will remove the trailing whitespace from the string